// Seleziono il bottone
const btn = document.getElementById('discover-btn');

// Aggiungo un event listener (per ora farà solo un log in console)
btn.addEventListener('click', () => {
    console.log('Hai cliccato su Scopri di più!');
    // Qui potrai inserire, in futuro, la logica per scrollare o aprire modale, ecc.
});